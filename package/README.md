# yt-dlp

An agent built from a description by the Agent Builder, as an
Agent Package: an Agent Plugin carrying a draft Agent Descriptor (what
the agent does, refuses, needs and records) and an install file (what a
host must have and do). It carries 1 skill(s). Nobody has run
it: this is a draft to review, not a vouched-for agent.
